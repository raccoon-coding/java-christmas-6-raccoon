package christmas.view.OutputView;

import static christmas.view.OutputView.OutputConstants.EVENT_BADGE;

public class EventBadgeView {
    public void runUI(String eventBadge) {
        System.out.println(EVENT_BADGE);
        System.out.print(eventBadge);
    }
}
