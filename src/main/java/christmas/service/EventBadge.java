package christmas.service;

public class EventBadge {
    public void eventBadge(double discount){
        santa(discount);
        tree(discount);
        star(discount);
    }

    private void star(double discount) {
        if(discount > 5_000){

        }
    }

    private void tree(double discount) {
        if(discount > 10_000){

        }
    }
    private void santa(double discount) {
        if(discount > 20_000){

        }
    }
}
